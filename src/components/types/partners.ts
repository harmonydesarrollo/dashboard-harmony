export interface Partners {
  _id: string;
  img: string;
  title: string;
  description: string;
}

export interface UpdatePartners {
  img: string;
  title: string;
  description: string;
}

export interface CreatePartners {
  img: string;
  title: string;
  description: string;
}
